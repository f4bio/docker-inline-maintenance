docker-inline-maintenance
===

* alpine based
* `nano` as text editor
* `bash` and `fish` shells

## how to

```bash
$ docker run --rm -it -v some-volume:/workspace
```

```fish
~> nano /workspace/file
```
...
